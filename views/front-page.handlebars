<div class="container">
    {{#each notes as |note|}}
    <div class="card">
        <div class="card-header">
            {{note.title}}
        </div>
        <div class="card-body">
            {{note.description}}
        </div>
    </div>
    {{/each}}
</div>